import styled from "styled-components";

const ButtonGroup = styled.div`
  display: flex;
  gap: 3rem;
  align-items: center;
  margin-top: auto;
  line-height: normal;
`;

export default ButtonGroup;
